name: Deploy to Streamlit Cloud

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Lint with flake8 (opcional)
      run: |
        pip install flake8
        # Para erros de sintaxe ou problemas indefinidos
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        # Advertências (descomente se quiser)
        # flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
      continue-on-error: true
    
    - name: Test imports
      run: |
        python -c "import streamlit; import pandas; import requests; print('✅ All imports OK')"

  deploy:
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Deploy notification
      run: |
        echo "🚀 Deploy automático será feito pelo Streamlit Cloud"
        echo "📦 Commit: ${{ github.sha }}"
        echo "👤 Author: ${{ github.actor }}"
